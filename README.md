# llm-benchmarking-py

## Usage

Build:

```shell
uv sync
```

Run Main:

```shell
uv run main
```

Run Unit Tests:

```shell
uv run task test
```

Run Benchmarking (3 runs, metrics written to `artemis_results.csv`):

```shell
uv run task benchmark
```
